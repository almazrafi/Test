name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Build
        run: sleep 10

  test:
    runs-on: macos-latest
    needs: build
    steps:
      - name: Test
        run: sleep 10
